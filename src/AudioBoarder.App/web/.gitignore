# Vite source for the embedded Excalidraw whiteboard.
# The BUILT bundle is committed at ../Assets/web (vendored, shipped with the app).
# Everything below is local-only tooling and must not be committed.
node_modules/
preview.log
*.tsbuildinfo

# Local font staging — re-copied from the npm package by build-bundle.ps1.
public/fonts/
