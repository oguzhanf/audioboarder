{
  "name": "audioboarder-whiteboard",
  "version": "1.0.0",
  "private": true,
  "description": "Embedded SVG architecture canvas for AudioBoarder (hosted in WebView2).",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "preview": "vite preview --port 5566 --strictPort"
  },
  "devDependencies": {
    "playwright": "^1.60.0",
    "vite": "^8.2.2"
  }
}
